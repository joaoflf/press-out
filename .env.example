PORT=8080
DATA_DIR=./data
DB_PATH=./data/press-out.db
# Path to Google Cloud service account JSON key (required for pose estimation)
# The file must already exist on the machine. The Go client library reads it automatically.
GOOGLE_APPLICATION_CREDENTIALS=
