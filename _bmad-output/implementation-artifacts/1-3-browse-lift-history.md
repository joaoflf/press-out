# Story 1.3: Browse Lift History

Status: ready-for-dev

## Story

As a lifter,
I want to see a list of all my uploaded lifts with their date and lift type,
so that I can find and review any previous lift.

## Acceptance Criteria (BDD)

1. **Given** the lifter has uploaded one or more lifts, **When** they visit the lift list page, **Then** all lifts are displayed in reverse chronological order (newest first), **And** each lift row shows the lift type (Snatch, Clean, or Clean & Jerk) and date, **And** each lift row shows a thumbnail image when available, **And** each row is tappable (full row is the tap target), **And** the page loads within 1 second (NFR2)

2. **Given** the lifter has no uploaded lifts, **When** they visit the lift list page, **Then** an empty state is displayed (no error, no broken layout), **And** the upload button remains visible and functional

## Tasks / Subtasks

- [ ] Create `web/templates/partials/lift-list-item.html` (AC: 1)
  - [ ] Normal state: thumbnail image (left, if `thumbnail.jpg` exists) + lift type text + formatted date
  - [ ] Full row is an anchor/link wrapping the content — tappable to navigate to `/lifts/{id}`
  - [ ] Minimum 44x44px touch target (UX-DR16)
  - [ ] Lift type displayed as readable text: "Snatch", "Clean", "Clean & Jerk"
  - [ ] Date formatted for readability (e.g., "Mar 15, 2026")
  - [ ] Thumbnail served from `/data/lifts/{id}/thumbnail.jpg` (or placeholder if not yet available)
  - [ ] Processing state variant: lift type + date + compact pipeline indicator (for Story 2.1 — add placeholder now)

- [ ] Update `internal/handler/lift.go` — `HandleListLifts` (AC: 1, 2)
  - [ ] Query all lifts via sqlc `ListLifts` (ordered by created_at DESC)
  - [ ] For each lift, check if `thumbnail.jpg` exists in the lift directory via `storage.LiftFile()`
  - [ ] Pass lift data + thumbnail availability to template
  - [ ] Handle empty state (zero lifts)

- [ ] Update `web/templates/pages/lift-list.html` (AC: 1, 2)
  - [ ] Loop over lifts and render `lift-list-item` partial for each
  - [ ] Show empty state message when no lifts exist (friendly text, no error styling)
  - [ ] Upload button always visible regardless of lift count

- [ ] Add static file serving for lift data directory (AC: 1)
  - [ ] Serve `data/lifts/` directory so thumbnails and videos are accessible via URL
  - [ ] Add route or file server in `internal/handler/routes.go`

- [ ] Write tests (AC: 1, 2)
  - [ ] Test GET / with lifts returns them in reverse chronological order
  - [ ] Test GET / with no lifts returns empty state
  - [ ] Test lift list item renders lift type and date correctly
  - [ ] Test thumbnail is shown when file exists

## Dev Notes

- Lift list items have two visual states: normal (with thumbnail) and processing (with compact pipeline indicator). This story implements the normal state. The processing state will be completed in Story 2.1.
- Thumbnails are generated by the crop stage (Story 2.4). Until processing completes, the thumbnail won't exist — handle this gracefully with a placeholder or no-image state.
- The list item is a full-row tap target that navigates to the lift detail page. Use an `<a>` tag wrapping the entire row content.
- Date formatting: use Go's `time.Format` with a human-readable layout. Store as RFC3339 in DB, format for display in template.
- Static file serving for the data directory must be added so thumbnails and videos are accessible from the browser.

### Project Structure Notes

Files to create:
- `web/templates/partials/lift-list-item.html` — list row partial

Files to modify:
- `internal/handler/lift.go` — enhance HandleListLifts with thumbnail checking
- `internal/handler/routes.go` — add data directory file serving
- `web/templates/pages/lift-list.html` — add lift list rendering loop and empty state

### References

- [Source: architecture.md#Frontend Architecture] — template organization, partials
- [Source: architecture.md#Data Architecture] — lift-ID directory, thumbnail.jpg
- [Source: epics.md#Story 1.3] — acceptance criteria
- [Source: ux-design-specification.md] — UX-DR9 (lift list item states), UX-DR16 (touch targets)

## Dev Agent Record

### Agent Model Used
### Completion Notes List
### Change Log
### File List
